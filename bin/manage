#!/bin/sh

BIN_PATH=$(cd `dirname $0` && pwd)
ROOT_PATH=`dirname "$BIN_DIRECTORY"`
DJANGO_PATH="$ROOT_PATH/.virtualenvs/development/bin/django-admin.py"
DJANGO_SETTINGS_MODULE=takestock.settings.development "$DJANGO_PATH" $@
